import {
  currentYear,
  currentMonth,
  currentDay,
  currentDate,
  lastYear,
  nextYear,
} from '../utils/dates';

//Bare URL
const baseUrl = 'https://api.rawg.io/api';

//Popular Games
const popularGames = 'https://api.rawg.io/api/games';
